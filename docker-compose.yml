services:
  keycloak:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: identity-server-keycloak

    command: start-dev

    env_file:
      - .env

    ports:
      - "8080:8080"